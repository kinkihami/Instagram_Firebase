import 'package:flutter/material.dart';

class ScreenLike extends StatelessWidget {
  const ScreenLike({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text('Like')),
    );
  }
}
